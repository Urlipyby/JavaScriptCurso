const salaDePython = ['Maria', 'Elena', 'Maicon']

const salaDeJS = ['Roberto', 'Ana', 'Yngrid']

const salasUnificadas = salaDePython.concat(salaDeJS)

console.log(`Os alunos que verão a palestra são: ${salasUnificadas}`)